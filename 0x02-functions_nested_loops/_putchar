#include <unistd.h>

/**
* _putchar - writes the character c to stdout
* @c: The charcater to print
*
*Return: On succes 1.
*On error, -1 is returned, and error is set approprtaely.
*/
int _putchar(char c)
{
return (write(1, &c, 1));
}
